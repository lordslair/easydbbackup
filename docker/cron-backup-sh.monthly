#!/bin/bash

[[ $SQLITE_DUMP  == 'True' ]] && /usr/bin/backup-sqlite.sh  monthly
[[ $REDIS_DUMP   == 'True' ]] && /usr/bin/backup-redis.sh   monthly
[[ $MYSQL_DUMP   == 'True' ]] && /usr/bin/backup-mysql.sh   monthly
[[ $MONGODB_DUMP == 'True' ]] && /usr/bin/backup-mongodb.sh monthly