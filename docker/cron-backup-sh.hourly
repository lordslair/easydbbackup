#!/bin/bash

[[ $SQLITE_DUMP  == 'True' ]] && /usr/bin/backup-sqlite.sh  hourly
[[ $REDIS_DUMP   == 'True' ]] && /usr/bin/backup-redis.sh   hourly
[[ $MYSQL_DUMP   == 'True' ]] && /usr/bin/backup-mysql.sh   hourly
[[ $MONGODB_DUMP == 'True' ]] && /usr/bin/backup-mongodb.sh hourly